---

- name: move haproxy config file to bastion
  copy:
    src: haproxy.cfg
    dest: /etc/haproxy/haproxy.cfg
    force: yes
    backup: yes

- name: enable haproxy
  systemd:
    enabled: yes
    name: haproxy

- name: Restart haproxy
  systemd:
    state: restarted
    name: haproxy
