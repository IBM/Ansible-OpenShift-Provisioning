--- 

- hosts: bastion_server
  become: true
  tasks:

  - name: update repository index
      dnf:
        update_cache: yes

  - name: install httpd
      dnf:
        name: httpd
        state: latest

## Not sure if this will work, especially after running once. Check this page out if encountering problems:  
##https://dmsimard.com/2016/03/15/changing-the-ssh-port-with-ansible/

  - name: Change ssh port to 8080
      set_fact:
        ansible_port: 8080

  
